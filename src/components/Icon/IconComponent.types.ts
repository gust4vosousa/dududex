export interface IIconProps {
  icon: EIcons
}

export enum EIcons {
  COMPARE = 'compare',
  DARK_MODE = 'dark_mode',
  HOME = 'home',
  LIGHT_MODE = 'light_mode',
  SEARCH = 'search',
  WARNING = 'warning',
}
